services:
  - type: web
    name: livros-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist/m7-chamados-angular/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
